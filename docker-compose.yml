version: "3.8"

services:
  cmo-summit:
    build: .
    container_name: cmo-summit
    restart: always
    ports:
      - "5173:5173"
    networks:
      - rede-proxy

networks:
  rede-proxy:
    external: true
